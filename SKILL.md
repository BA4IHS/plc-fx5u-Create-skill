---
name: plc-fx5u-generator
description: 三菱 FX5U PLC 程序 CSV 文件生成器。当用户提到 PLC、FX5U、FX3U、FX2N、三菱、梯形图、GX Works3、GX Works2、GX2转GX3、老程序迁移、电机控制、故障报警、定时器、互锁、启动停止、抢答器、送料小车、电梯控制、IO点表，或描述任何工业自动化控制需求（启停、正反转、闪烁、循环、限位、急停）时，立即使用此 skill。即使用户只说了"生成PLC程序"而没有明确指定型号，也应触发此 skill。
---

# 三菱 FX5U PLC 程序生成器

根据用户描述的工业控制需求，生成符合 GX Works3 导入规范的 PLC 程序 CSV 文件。

## 工作流

### 第 1 步：分析控制需求

从用户描述中识别以下要素：

- **输入信号**：启动按钮、停止/急停按钮、限位开关、传感器、选择开关等
- **输出执行**：电机、指示灯、蜂鸣器、阀门、继电器等
- **时序逻辑**：延时启动、间歇运行、循环控制、定时切换等
- **互锁/保护**：正反转互锁、限位保护、急停保护、故障处理等
- **特殊功能**：闪烁、计数、移位、比较等

如果需求描述模糊，主动向用户提问澄清。尤其要确认：
- 急停后的复位方式（自动复位还是手动复位）
- 循环次数（无限循环还是指定次数）
- 时序精度要求（100ms 时基是否满足）

### 第 2 步：分配软元件

根据需求设计软元件分配方案。向用户展示分配方案，确认后再生成文件。

软元件命名规范：
- **输入 X**：X0~X177（八进制），用于按钮、传感器、限位等
- **输出 Y**：Y0~Y177（八进制），用于电机、指示灯、蜂鸣器等
- **内部继电器 M**：M0~M7679，用于中间状态标志
- **定时器 T**：T0~T511（100ms 时基），T 编号 = 秒数 × 10
- **计数器 C**：C0~C255（16位），C 编号
- **数据寄存器 D**：D0~D7999
- **报警器 F**：F0~F2047（特殊报警继电器）
- **特殊继电器 M**：SM0~SM9999（系统状态）
- **特殊寄存器 SD**：SD0~SD9999（系统数据）

分配原则：
1. 输入/输出从 X0、Y0 开始连续分配
2. 内部继电器 M0 通常用作"运行标志"
3. 定时器按逻辑顺序分配，避免重复使用同一编号
4. SET 和 RST 配对使用于需要自锁保持的场合
5. OUT 指令用于直接条件输出（无需自锁的场景）
6. ANI 用于常闭触点串联，实现互锁

### 第 3 步：选择生成方式

向用户确认生成方式：
- **方式 A（直接 CSV）**：直接生成 UTF-16LE CSV 文件，适合简单程序
- **方式 B（Python 脚本）**：生成 Python 脚本再运行产生 CSV，适合复杂程序，可复现可修改

如果用户没有明确偏好，默认选择方式 B（Python 脚本），因为它更可靠，避免直接写 CSV 时出现的编码问题。

### 第 4 步：生成文件

严格按照 CSV 格式规范生成文件。详细规范见 `references/csv-format-spec.md`。

核心要求：
- 编码：UTF-16LE with BOM
- 分隔符：制表符 (Tab)
- 每列用双引号包围
- 文件头 3 行：工程名、机型信息、列标题
- 扩展格式：定时器/计数器参数单独行，ANS 指令 3 行（`ANS Tn`/`Kn`/`Fn`），步号为空字符串
- 程序以 END 指令结束
- **注解列必须为空**：第 7 列（注解）必须始终为 `""`（空字符串），禁止填入任何文本。填入注解会导致 GX Works3 导入失败，报错"无法读取软元件"或"梯形图配置不正确"

### 第 5 步：验证和报告

生成文件后：
1. 向用户报告生成的文件名和保存位置
2. 列出完整的软元件分配表
3. 说明程序的控制流程
4. 提示安全设计要点
5. 询问是否需要修改

## CSV 格式要点

7 列表头：`步号` | `行间声明` | `指令` | `I/O(软元件)` | `空白栏` | `PI声明` | `注解`

格式变体（详见 `references/csv-format-spec.md`）：
- **扩展格式**：定时器/计数器参数单独一行，参数行步号为空
- **三行扩展格式**：ANS（`ANS Tn`→`Kn`→`Fn`）、算术运算（`ADDP S1`→`S2`→`D`）
- **四行格式**：FROM/TO 模块读写（`FROM K0`→`K29`→`D10`→`K1`）
- **六行格式**：ADPRW MODBUS 通信
- **MC 二行格式**：`MC N0`→`M998`
- **紧凑格式**：所有参数与指令同行（历史兼容）

常数值：
- 十进制：`K10`, `K-16`, `K-600`（支持负数）
- 十六进制：`H0D`, `HFF`
- 浮点数：`E1.23`, `E-4.56`

## 指令参考

常用指令详见 `references/instruction-set.md`。

## 程序示例

典型程序结构参见 `references/program-examples.md`。

## 生成规范

### 方式 A：直接生成 CSV

使用 `scripts/write_csv.py` 工具脚本，将程序行数据写入 CSV。

```bash
python scripts/write_csv.py <输出路径> <程序名>
```

脚本会从标准输入读取程序行定义（JSON 格式），自动处理 UTF-16LE 编码和格式。

### 方式 B：生成 Python 脚本

生成的 Python 脚本必须：
1. 使用 `scripts/write_csv.py` 中的 `write_plc_csv()` 函数，或内联相同的编码逻辑
2. 包含完整的 `rows` 列表定义
3. 默认输出到用户当前打开的文件夹（即当前工作目录）；如果获取不到当前文件夹，则回退到用户桌面
4. 文件命名格式 `ProgPou_<描述>.csv`
5. 运行脚本生成最终 CSV 文件

脚本参考模板（关键编码部分）：

```python
import json, sys, os

# 输出路径：优先当前工作目录，回退到桌面
output_dir = os.getcwd() if os.path.exists(os.getcwd()) else os.path.join(os.path.expanduser("~"), "Desktop")
output_path = os.path.join(output_dir, "ProgPou_<描述>.csv")

# 程序行定义: [步号, 行间声明, 指令, I/O软元件, 空白栏, PI声明, 注解]
rows = [
    ["0", "", "LDP", "X0", "", "", ""],
    ["2", "", "SET", "M0", "", "", ""],
    # ... 更多行
    ["N", "", "END", "", "", "", ""]
]
# 写入 CSV：将 rows 传给 write_plc_csv 或使用内置编码逻辑
```

编码实现要点：
- `open(path, 'wb')` 写入 `b'\xff\xfe'` (BOM)
- 表头 3 行 → 数据行 → 全部 `encode('utf-16-le')` 后写入
- 每列用 `f'"{col}"'` 包围，Tab 连接

## 安全设计原则

生成 PLC 程序时必须遵循：
1. **急停优先**：急停按钮必须能无条件切断所有输出，同时复位所有中间继电器和定时器
2. **互锁保护**：正反转/上下行等相反动作必须有硬件互锁（常闭触点）和软件互锁
3. **限位保护**：行程极限位置必须有硬限位开关直接切断输出
4. **上电初始化**：确保 PLC 上电后所有输出处于安全状态
5. **故障处理**：关键设备应有故障检测和报警机制

## GX Works2 / FX3U → GX Works3 / FX5U 差异

当用户提到 FX3U、FX2N、GX Works2、GX2转GX3、老程序迁移等关键词时，注意以下差异：

### 特殊继电器/寄存器映射

FX3U 的 M8000~M8511 / D8000~D8511 在 FX5U 中对应 SM / SD 系列：

| FX3U (GX Works2) | FX5U (GX Works3) | 功能 |
|------------------|------------------|------|
| M8000 | SM400 | 常 ON |
| M8001 | SM401 | 常 OFF |
| M8002 | SM402 | 初始脉冲（RUN 后第一个扫描周期 ON） |
| M8005 | SM500 | 电池电压低 |
| M8011 | SM410 | 100ms 时钟 (10Hz) |
| M8012 | SM411 | 10ms 时钟 (100Hz) |
| M8013 | SM412 | 1s 时钟 (1Hz) |
| M8014 | SM413 | 2s 时钟 / 1min 时钟 |
| M8020~M8029 | — | FX5U 无对应（部分功能改用指令实现） |
| D8010 | SD0 | 当前扫描时间 (0.1ms) |
| D8011 | SD1 | 最小扫描时间 |
| D8012 | SD2 | 最大扫描时间 |
| M8200~M8234 | SM200~SM255 | 计数器 C200~C255 方向控制（ON=减计数） |

> 完整映射表见 `references/instruction-set.md` 附录。

### 指令差异

| 差异点 | FX3U (GX Works2) | FX5U (GX Works3) |
|--------|------------------|------------------|
| 四则运算 | ADD/SUB/MUL/DIV 指令块 | 可用 `+ - * /` 运算符直接输入 |
| INC/DEC | 仅 16 位有符号 | 需区分 `INC_U`(无符号)、`DINC`(32位)、`DDEC`(32位) |
| TO 参数顺序 | `TO n1 n2 S n4` | 参数顺序有变化 |
| LAD 内嵌 ST | 不支持 | 梯形图中可内嵌 ST 程序段 |
| FB 功能块 | 占用主程序空间 | 独立存储区，不占程序空间 |
| 计数器方向 | M8200~M8234 控制 C200~C234 | SM200~SM255 控制 C200~C255 |

### 软件适用

- **GX Works2**：FX 系列（除 FX5U）、Q 系列、L 系列
- **GX Works3**：FX5U (iQ-F)、R 系列 (iQ-R)
- FX5U **只能用** GX Works3 编程

## 程序命名

生成的 CSV 文件命名格式：`ProgPou_<功能描述>.csv`
例如：`ProgPou_motor_interlock.csv`、`ProgPou_fault_alarm.csv`

## 重要提示

- 定时器时基为 100ms，K 值 = 秒数 × 10。例如 5 秒 = K50
- 步号必须为字符串，递增不重复。生成时偶数递增（0, 2, 4, 6, ...），GX Works3 导出可能含奇数
- 参数行步号为空字符串 ""
- 列标题和机型信息行必须完整，否则 GX Works3 无法正确导入
- 软元件编号 X/Y 使用八进制，M/T/C/D/F 使用十进制
- 程序结束必须有 END 指令
- K 值支持负数（如 `K-16`），H 值支持十六进制（如 `H0D`）
- 特殊继电器 SM412（1s 时钟）、SM414（PID 触点）可直接用 LD/AND 引用
- ZRST 用于批量复位：`ZRST D1 D2` 将 D1~D2 区间全部清零（2 行格式）
- FROM/TO 用于读写模块 BFM：4 行格式（模块号→BFM号→寄存器→字数）
- E 常量表示浮点数：`E1.23`、`E-4.56`
